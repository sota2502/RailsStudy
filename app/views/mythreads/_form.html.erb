<%= form_for(@mythread) do |f| %>
  <% if @mythread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mythread.errors.count, "error") %> prohibited this mythread from being saved:</h2>

      <ul>
      <% @mythread.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
